# Desktop UI verification — fase4-snippets-manager

- Botón Snippets en `sidebar-footer` (prefs del footer intactas)
- Modal: chips (+ al inicio, x dentro), lista plana, búsqueda, form in-modal
- Copy via clipboard; edit/delete en menú ⋮ B3; delete A1
- Sin shortcut; sin write PTY
- `npm run build` OK
- CSS scoped a `#snippets-modal` / `.snippets-*` (sin tocar estilos globales de prefs)
